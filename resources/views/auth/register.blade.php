
{{-- <form action="{{ route('mail') }}" method="post">
    @csrf
    <div class="form-group">
        email : <input type="email" name="name" value="{{ old('email') }}">
    </div>
    <input type="submit" value="送信">
</form> --}}
<a href="{{ route('welcome') }}">戻る</a>       
    